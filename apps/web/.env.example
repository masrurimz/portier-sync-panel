# Copy to .env before running `bun dev:cf` (alchemy dev).
# CORS_ORIGIN is required by the Cloudflare Worker binding; for local dev
# it should match the vite dev server port (default 3001).
CORS_ORIGIN=http://localhost:3001
