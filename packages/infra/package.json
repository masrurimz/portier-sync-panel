{
  "name": "@portier-sync/infra",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "alchemy dev",
    "deploy": "alchemy deploy",
    "destroy": "alchemy destroy",
    "check:types": "tsc --noEmit --skipLibCheck --module ESNext --moduleResolution bundler --target ESNext alchemy.run.ts",
    "check:lint": "oxlint alchemy.run.ts"
  },
  "dependencies": {
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@portier-sync/config": "workspace:*",
    "alchemy": "catalog:",
    "typescript": "^5"
  }
}
